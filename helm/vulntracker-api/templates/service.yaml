apiVersion: v1
kind: Service
metadata:
  name: {{ include "vulntracker-api.fullname" . }}
  labels:
    {{- include "vulntracker-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vulntracker-api.selectorLabels" . | nindent 4 }}
